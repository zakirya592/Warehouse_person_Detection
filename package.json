{
  "name": "safety-detection",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db:generate": "python -m prisma generate",
    "db:push": "npx prisma db push",
    "db:migrate": "npx prisma migrate dev",
    "db:studio": "npx prisma studio"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
